#!/usr/bin/python3
import re
"""
validate utf8 module
"""


def validUTF8(data):
    """determines if a given data set represents a valid UTF-8 encoding"""
    regex = '^(1)+'
    num_bytes = 0
    bytes_left = 0
    init = True
    return True
